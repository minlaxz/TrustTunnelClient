[package]
name = "trusttunnel-subscription-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["lib", "staticlib", "cdylib"]

[dependencies]
trusttunnel-subscription = { path = "../subscription" }
