interface:
  display_name: "TrustTunnelClient"
  short_description: "Repo-specific patterns and workflows for TrustTunnelClient"
  default_prompt: "Use the TrustTunnelClient repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true